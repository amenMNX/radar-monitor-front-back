# `com/radar` Test Package Folder

This folder continues the Java test package path for the radar monitoring backend.

The actual test classes are inside:

```text
com/radar/monitoring
```

This folder exists to keep test package names aligned with production package names.
