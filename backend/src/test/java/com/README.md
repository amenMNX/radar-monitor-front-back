# `com` Test Package Folder

This folder is part of the Java test package path.

It mirrors the production package structure:

```text
com.radar.monitoring
```

There is no test logic directly in this folder. Test classes are placed deeper in the package tree.
