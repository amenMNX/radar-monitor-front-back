# `src/test/java` Folder

This folder contains Java test source files.

It follows the same package structure as the production Java code:

```text
com.radar.monitoring
```

Tests in this folder can use JUnit and Spring Boot Test to verify backend behavior.

Example test types that belong here:

- Application startup tests
- Controller tests
- Service tests
- Repository tests
- Integration tests
