# `com` Package Folder

This folder is the first part of the Java package name.

The backend package starts with:

```text
com.radar.monitoring
```

In Java projects, package folders usually mirror the package name declared at the top of each `.java` file. This folder exists to support that package structure.

There is no application logic directly inside this folder. The real backend code is deeper in `com/radar/monitoring`.
