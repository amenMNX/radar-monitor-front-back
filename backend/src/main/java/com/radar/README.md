# `com/radar` Package Folder

This folder is the second part of the backend Java package name.

The full package path is:

```text
com.radar.monitoring
```

This folder groups code that belongs to the radar monitoring project. It does not contain direct application logic by itself. The real Spring Boot classes are inside the `monitoring` folder.
